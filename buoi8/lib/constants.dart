import 'package:flutter/material.dart';

const Color login_bg = Color(0xFF00C470); // Màu nền Login (xanh lá)
const Color signup_bg = Color(0xFF000A54); // Màu nền Signup (xanh đậm)

const double defaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 300); // Thời gian animation